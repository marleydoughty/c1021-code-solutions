/* exported getFirstChar */

// create storage space for output (empty string);
// once reached the first character/first index, assign that value into the output;
// return value of the output;

function getFirstChar(string) {
  var output = '';
  output = string[0];
  return output;
}
