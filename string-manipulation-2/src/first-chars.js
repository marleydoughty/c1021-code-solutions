/* exported firstChars */
/* look at each index in the string starting at index 0;
at a certain index length- stop;
return the output */
function firstChars(length, string) {
  return string.slice(0, length);
}
